\section{Claim Boundary and Limitations}

The supplement closes by restating the boundary in table form. The main paper's evidence supports
a scoped diagnostic-checker story: primitive packages should be treated as candidates until named
Newton probes accept them under recorded settings. The evidence does not support stronger claims
about deployment, certification, real-world transfer, or broad robot operation.

\begin{table}[t]
\centering
\small
\caption{Boundary table. It maps supported statement types to unsupported
overreadings and is not a copy of a main-paper result table.}
\label{tab:supp-claim-boundary}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.48\linewidth}@{\hspace{0.05\linewidth}}>{\raggedright\arraybackslash}p{0.35\linewidth}@{}}
\toprule
Supported statement & Unsupported overreading \\
\midrule
Recorded diagnostic predicates ran under scoped settings & General benchmark superiority \\
Generated packages were consumed by configured smoke scenes & Deployment readiness \\
Link ownership can be checked structurally & Whole-robot collision quality \\
Generated self-collision filter pairs are recorded for a smoke scene & Calibrated robot contact behavior \\
Safety-affecting artifacts require review & Safety certification \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Unsupported claim classes.}
The supplement should not be read as claiming a production compiler, calibrated default selector,
complete replacement of convex decomposition, or broad primitive speed superiority. It also does
not claim whole-robot Franka joint-performance validity, manipulation validity, real-world
transfer, deployment readiness, safety certification, or a safety proof. The phrase diagnostic
checker is used deliberately instead of formal-verification wording.

\paragraph{Evidence needed for stronger benchmark claims.}
A broader benchmark claim would require a fixed asset split, source and license metadata, paired
asset-level metrics, baseline versions and parameters, Newton version and solver settings,
hardware, seeds, fallback examples, failure examples, and statistical treatment for comparative
claims. The current supplement does not add such a benchmark.

\paragraph{Evidence needed for stronger robot claims.}
A stronger robot claim would require more robot assets or tasks, proof that generated packages are
consumed by robot task runtimes, link/joint graph preservation, source-shape suppression, generated
self-collision accounting, gravity holds, scripted trajectories, self-collision sanity,
end-effector sanity, and contact-operation smokes when contact operation is claimed. The current
Franka material is a scoped generated-package consumption smoke.

\paragraph{Evidence needed for safety or deployment claims.}
Simulator diagnostics can support review of safety-affecting artifacts, but they do not create a
deployment protocol or certification standard. A safety or deployment claim would require a
separate verification standard, environment assumptions, runtime monitoring plan, human review
procedure, and external evidence record.

\begin{table}[t]
\centering
\small
\caption{Reviewer audit checklist for stronger future claims. The supplement records these as
missing evidence classes rather than implying that they are already satisfied.}
\label{tab:supp-future-evidence-checklist}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.22\linewidth}@{\hspace{0.03\linewidth}}>{\raggedright\arraybackslash}p{0.47\linewidth}@{\hspace{0.03\linewidth}}>{\raggedright\arraybackslash}p{0.15\linewidth}@{}}
\toprule
Claim class & Needed evidence & Status \\
\midrule
Default selector & calibrated thresholds, held-out assets, paired fallbacks & not claimed \\
Broad benchmark & fixed asset split, baseline parameters, statistics & not claimed \\
Robot operation & contact-operation smokes and end-effector checks & not claimed \\
Deployment & deployment protocol and runtime monitoring assumptions & not claimed \\
Safety certification & external standard and certification evidence & not claimed \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Why this checklist appears in the supplement.}
The checklist is included to make review easier, not to create future-work claims. A reviewer can
compare any tempting interpretation against the table: if a statement would require one of these
missing evidence classes, it is outside the current paper. This convention is especially important
for primitive collider packages because they are safety-affecting artifacts that may look simple
while changing simulated body state, contact behavior, or robot link ownership.

\paragraph{Final reading rule.}
The intended conclusion is conservative: candidate primitive packages are useful because they are
editable and can be fast after acceptance, but they should be promoted only after named diagnostic
checks accept them. The supplement explains how those checks are assembled; it does not broaden what
the checks prove.

\paragraph{Reviewer audit walkthrough.}
When reading a reported accept or failure, a reviewer can apply the same sequence used throughout
the supplement. First, identify the candidate package and the generator lane that produced it; this
prevents a fallback or dependency gap from being mistaken for a simulated rejection. Second, inspect
the body or link attachment map. For rigid objects this usually means confirming that all generated
primitives enter the intended Newton body. For articulated assets this means checking that every
primitive keeps the source link owner and that no cross-link merge is silently introduced. Third,
inspect the configured predicate family rather than a generic success word: drop/settle, stack-or-
slide, sphere-rain, and generated-package consumption answer different questions.

The final audit step is to match the visual explanation back to the record class that owns the
evidence. A rendered scene can show the diagnostic object, support surface, robot link package, or
failure symptom, but it does not replace a dated report. Likewise, a compact table can summarize a
gate, but it does not create threshold authority. Threshold values belong in configs and records;
figures in this supplement explain the reading order. If a desired interpretation needs more assets,
more robot tasks, contact-operation smokes, calibrated thresholds, or an external safety standard,
then it is a future evidence requirement rather than a conclusion of this paper.
