\section{Experiments}
\label{sec:experiments}

We report scoped evidence from the current bootstrap.
Phase~0 targets 5--10 provenance-clear assets with paired baselines; the tables below summarize the recorded ACCV proof point without treating unrun lanes as hidden successes.

\subsection{Mechanism study: capped bed versus Franka cylinders}
\label{sec:exp-mechanism}

For the recorded capped scope, the bed selected-cylinder package fails drop/settle with residual final speed about $0.082$\,m/s, while a recorded Franka cost-guided cylinder package passes with about $0.00071$\,m/s.
The same target bed cylinder passes as an isolated primitive; the failure is a full-compound package effect.
COM/inertia sensitivity is the strongest supported mechanism; mass alone and contact/floor are rejected as sole explanations in the dated audit.
Figure~\ref{fig:bed-franka-mechanism} summarizes the mechanism audit.
The important point is not that cylinders are generally bad.
The recorded Franka package contains small link-local cylinders and passes its bounded task smoke,
while the bed case fails only when the large flat primitive participates in the full compound body.
This is the first piece of evidence for the paper's main separation: the candidate shape and the
accepted simulation artifact are different objects.

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/generated/bed_franka_mechanism_diagnostic.pdf}
  \caption{Mechanism diagnostic for the capped bed/Franka cylinder records. The bed selected-cylinder package exceeds the recorded settle-speed gate in full-package context, while the scoped Franka cylinder package passes. The right panel records the audit conclusion: contact/floor and mass-only explanations are rejected as sole causes, while COM/inertia sensitivity in the full compound package is supported for this bounded case.}
  \label{fig:bed-franka-mechanism}
\end{figure}

\subsection{Diagnostic guard}
An opt-in package body-state guard falls back only the flagged capped bed package while keeping the unflagged Franka cylinder package in the recorded Newton task smoke.

\subsection{Collision-only throughput hook}
In one bed-aligned pressure scene with 32 accepted box primitives versus 32 convex64 mesh proxies, native primitives report median $2.21\times$ generated contacts per collide call and about $5.3\%$ lower collision-only wall time.
This is scoped collision-pipeline evidence, not a full-simulation speedup claim.

\subsection{Phase 0 rigid-asset diagnostic table}
Table~\ref{tab:phase0-grscenes-rigid} summarizes the current scoped Phase~0 rigid run over five materialized GRScenes assets.
The run records first-mesh bounding-primitive, CPD-style, and CoACD convex-mesh candidate lanes with Newton contact, drop/settle, stack-or-slide, and sphere-rain probes.
It also records V-HACD runtime evidence for all five assets, with V-HACD probe failures on bowl, cup, and tray.
We treat these V-HACD failures as recorded diagnostic outcomes: the packages are generated and mapped, but the Newton task probes reject them, supporting simulation-checked acceptance rather than geometry-only candidate generation.
It also includes one Franka link-aware package generation record, one Franka USD articulation smoke, and one generated-package Franka task smoke.
It is not a complete Phase~0 benchmark: selected V-HACD lanes still fail probes, the Franka package records a meshless link sentinel, and robot generated-package checks currently cover only one Franka smoke asset.

Figures~\ref{fig:phase0-overlays} and~\ref{fig:phase0-control-overlays} show why a table alone is insufficient.
Each row starts from default/render mesh prims loaded through the repo-local USD mirror, then shows the
candidate package geometry that the diagnostics consume.
The bowl, cup, and tray rows are especially informative: all three have V-HACD hull packages, so
the later failures are not dependency gaps or missing-baseline artifacts.
They are mapped collision packages that fail particular Newton probes.
The keyboard row is retained as a precision negative control: routine probes pass, but the recorded
policy still routes the asset to manual review rather than promoting it as a solved collider.

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/generated/phase0_asset_package_overlays.pdf}
  \caption{Deterministic Phase~0 asset/package overlays for the representative rigid prop, container, and contact-affordance rows. Columns show the input mesh and four generated candidate lanes. High-count lanes draw a readable representative subset with a visible $+N$ marker; counts below each package remain primitive or hull counts in the recorded package, not claims of physical acceptance.}
  \label{fig:phase0-overlays}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/generated/phase0_asset_package_control_overlays.pdf}
  \caption{Additional Phase~0 asset/package overlay rows for the stackable tray and the precision negative-control keyboard. The keyboard input column shows aggregated USD default/render mesh prims for recognizability; high-count keyboard package cells draw a labeled representative local marker with a visible $+N$ overflow count. The split keeps the control rows readable while preserving the same candidate-lane ordering and recorded count semantics as Figure~\ref{fig:phase0-overlays}.}
  \label{fig:phase0-control-overlays}
\end{figure}

Figure~\ref{fig:phase0-outcome-matrix} expands the table into the full rigid diagnostic matrix.
The single-convex lane is intentionally visible as gray fallback cells.
The green cells show that simple bounding packages can survive these smoke probes, but this is not
a superiority claim: bounding boxes are coarse and can be inappropriate for precision or
affordance-sensitive tasks.
The red cells show the opposite failure mode: detailed decomposition can look geometrically
plausible and still produce an unstable support, floor breach, or non-settled state.

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/generated/phase0_outcome_matrix.pdf}
  \caption{Outcome matrix for the scoped five-asset rigid run. Green cells are accepted probe outcomes, red cells are recorded failures, gray cells are explicit fallbacks, and light cells are not applicable. Fallback and failure are preserved as evidence rather than omitted from the table.}
  \label{fig:phase0-outcome-matrix}
\end{figure}

\begin{table}[H]
  \centering
  \footnotesize
  \begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.17\textwidth}>{\raggedright\arraybackslash}p{0.24\textwidth}>{\raggedright\arraybackslash}p{0.22\textwidth}>{\raggedright\arraybackslash}p{0.25\textwidth}@{}}
    \toprule
    Asset role & Candidate lanes & \simchecked{} outcome & Recorded fallback/failure \\
    \midrule
    rigid prop & bbox 1; CoACD 1; V-HACD 1; CPD 12 & generated lanes pass & single-convex fallback \\
    container & bbox 1; CoACD 9; V-HACD 16; CPD 16 & bbox pass; sphere pass & single-convex fallback; CoACD/CPD/V-HACD drop+stack failures \\
    contact affordance & bbox 1; CoACD 5; V-HACD 16; CPD 16 & bbox pass; sphere pass & single-convex fallback; CoACD/CPD/V-HACD drop; CPD stack failure \\
    stackable & bbox 1; CoACD 1; V-HACD 16; CPD 16 & bbox, CoACD, CPD pass & single-convex fallback; V-HACD drop failure \\
    precision control & bbox 1; CoACD 1; V-HACD 16; CPD 12 & tested lanes pass & single-convex fallback; manual review required \\
    \bottomrule
  \end{tabular}
  \caption{Scoped Phase~0 GRScenes diagnostic summary. Counts are primitive or hull counts in the recorded package lanes; fallback and failure entries are recorded outcomes, not hidden exclusions.}
  \label{tab:phase0-grscenes-rigid}
\end{table}

\subsection{Collision-probe scene renderings}
\label{sec:exp-collision-scenes}

Figure~\ref{fig:phase0-collision-scenes} renders the diagnostic context for the three most
evidence-bearing rigid assets.
The left column shows the V-HACD package that was generated and mapped.
The middle column summarizes the drop/settle state transition using recorded initial and final
heights, and the right column summarizes the stack-or-slide probe.
For the bowl, both drop/settle and stack/slide reject the V-HACD package.
For the cup and tray, the V-HACD stack/slide probe passes while drop/settle still rejects the
package.
That split is useful: the checker is not a blunt baseline filter.
It localizes which physical task failed and leaves passing probes visible.

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/generated/phase0_collision_probe_scenes.pdf}
  \caption{Collision-probe scene visualizations for the recorded V-HACD lanes on bowl, cup, and tray. These panels are deterministic diagnostic renderings from the Phase~0 report, not photorealistic evidence. The key point is that a generated and mapped package can pass one task while failing another.}
  \label{fig:phase0-collision-scenes}
\end{figure}

The collision-scene panels also explain why the paper treats V-HACD failures as favorable evidence
for the simulation-checked story.
If V-HACD had failed to run, the comparison would only show an engineering dependency gap.
Here the generated packages exist, map into Newton shapes, and then fail named probes with labels
such as \texttt{not\_settled}, \texttt{floor\_breach},
\texttt{excess\_horizontal\_slide}, and \texttt{probe\_below\_support}.
Those labels are exactly the reviewer-facing distinction we want: geometry generation is a
candidate source, while simulation acceptance is a separate decision layer.

\subsection{Failure labels and measured symptoms}
\label{sec:exp-failure-labels}

Table~\ref{tab:phase0-failure-labels} lists the recorded failure rows behind the red cells in
Figure~\ref{fig:phase0-outcome-matrix}.
The labels are not post-hoc prose annotations; they are emitted by the probe code from measured
state.
For example, the bowl V-HACD drop/settle row records \texttt{no\_descent},
\texttt{not\_settled}, and \texttt{floor\_breach} together with a final speed of
$5.493$\,m/s.
The same bowl V-HACD package then fails stack/slide because the probe moves $1.219$\,m across the
support.
The cup V-HACD package shows a different failure: the final height is close to the plane, but the
final speed still exceeds the settle gate.
The tray V-HACD row is again different: the stack/slide probe accepts the package, while
drop/settle rejects it for residual motion.
This heterogeneity is why the paper reports named probes instead of a single binary baseline score.

\begin{table}[t]
  \centering
  \footnotesize
  \begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.16\textwidth}>{\raggedright\arraybackslash}p{0.18\textwidth}>{\raggedright\arraybackslash}p{0.14\textwidth}>{\raggedright\arraybackslash}p{0.25\textwidth}>{\raggedright\arraybackslash}p{0.19\textwidth}@{}}
    \toprule
    Asset & Lane & Probe & Failure labels & Recorded symptom \\
    \midrule
    bowl & V-HACD & drop & no descent; not settled; floor breach & speed $5.493$\,m/s; final $z=0.517$ \\
    bowl & V-HACD & stack & excess slide; probe below support & slide $1.219$\,m; speed $0.018$\,m/s \\
    bowl & CoACD & drop & not settled; floor breach & speed $5.684$\,m/s; final $z=-0.138$ \\
    bowl & CoACD & stack & no final contact; excess slide; probe below support; not settled & slide $5.593$\,m; speed $9.373$\,m/s \\
    bowl & CPD-style & drop & not settled & speed $4.281$\,m/s; final $z=-0.851$ \\
    bowl & CPD-style & stack & excess slide; probe below support; not settled & slide $4.712$\,m; speed $9.703$\,m/s \\
    cup & V-HACD & drop & not settled & speed $0.086$\,m/s; final $z=-0.001$ \\
    cup & CoACD & drop & not settled; floor breach & speed $1.318$\,m/s; final $z=0.186$ \\
    cup & CPD-style & drop & not settled & speed $0.356$\,m/s; final $z=-0.016$ \\
    cup & CPD-style & stack & probe below support & slide $0.128$\,m; speed $0.002$\,m/s \\
    tray & V-HACD & drop & not settled & speed $0.204$\,m/s; final $z=0.007$ \\
    \bottomrule
  \end{tabular}
  \caption{Recorded failure labels and representative measured symptoms for the Phase~0 bowl, cup, and tray rows. These rows are generated-package diagnostic failures, not missing-baseline or dependency-gap cases.}
  \label{tab:phase0-failure-labels}
\end{table}

\subsection{Franka articulation smoke}
The Phase~0 follow-up report includes one Franka USD smoke asset.
The link-aware package generator detects 12 rigid-body links and 12 joint edges, emits 12 link-framed box primitives, and records zero cross-link merges.
The \texttt{panda\_link8} primitive is an explicit meshless sentinel because that link has no mesh in the source USD mirror.
The articulation smoke records joint-tree import, a short gravity hold, and a simple kinematic trajectory as passed.
The generated-package task smoke separately suppresses source USD shapes, attaches all 12 generated package primitives to Newton bodies, records zero missing body links, and passes the same short hold/trajectory probe.
This is generated-package consumption evidence for one Franka smoke asset, not whole-robot collider quality or manipulation evidence.

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/generated/franka_link_aware_task_scene.pdf}
  \caption{Franka link-aware generated-package task visual. The package covers 12 detected links with 12 primitives, preserves the meshless \texttt{panda\_link8} sentinel, suppresses source USD collision shapes in the generated-package task smoke, records zero missing body links, and installs 66 generated self-collision filter pairs. This supports package consumption only, not whole-robot collision quality or manipulation performance.}
  \label{fig:franka-task-scene}
\end{figure}

The robot result is deliberately narrow but important for the paper story.
Primitive packages for articulated assets must preserve link identity, because a visually plausible
whole-robot proxy can be unusable if it merges geometry across joints.
The link-boundary audit therefore checks the package before task execution: every primitive must
have one source link, use that link as its frame, and avoid cross-link merges.
The subsequent generated-package task smoke then verifies that the package is actually consumed by
Newton bodies with source USD collision shapes suppressed.
The recorded task has a short gravity hold and a simple trajectory only; it is a consumption and
boundary smoke, not manipulation evidence.
