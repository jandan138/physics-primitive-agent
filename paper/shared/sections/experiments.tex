\section{Experiments}
\label{sec:experiments}

The experiments test whether the proposed gate changes the story told by geometry alone.
We use the recorded five-asset Phase~0 rigid slice and one Franka smoke asset to ask three questions: can package context flip an otherwise plausible primitive, can generated baseline packages fail only after successful Newton mapping, and what ownership condition appears once the asset is articulated?

\subsection{Mechanism study: capped bed versus Franka cylinders}
\label{sec:exp-mechanism}

The first test isolates the paper's core failure mode: a primitive can be geometrically plausible and still become unacceptable only after it joins a package.
For the recorded capped scope, the bed selected-cylinder package fails drop/settle with residual final speed about $0.082$\,m/s, while a recorded Franka cost-guided cylinder package passes with about $0.00071$\,m/s.
The same target bed cylinder passes as an isolated primitive, so the failing variable is package context rather than the cylinder shape alone.
COM/inertia sensitivity is the strongest supported mechanism; mass alone and contact/floor are rejected as sole explanations in the recorded diagnostic checks.
Figure~\ref{fig:bed-franka-mechanism} summarizes the mechanism evidence.
The resulting takeaway is narrow but important: the acceptance gate catches a package-level body-state failure that a per-primitive geometry check would accept.

\begin{figure}[tbp]
  \centering
  \includegraphics[width=0.80\textwidth]{figures/generated/bed_franka_mechanism_diagnostic.pdf}
  \caption{Mechanism diagnostic for the capped bed/Franka cylinder records. In full-package context, the bed selected-cylinder package exceeds the recorded settle-speed gate, whereas the isolated target primitive and scoped Franka cylinder package pass their bounded checks. Recorded diagnostic checks reject contact/floor and mass-only explanations as sole causes and support COM/inertia sensitivity for this bounded case.}
  \label{fig:bed-franka-mechanism}
\end{figure}

\subsection{Diagnostic guard}
The guard test asks whether the checker can reject the failing context without discarding every primitive package of the same family.
In the recorded task path, the opt-in body-state guard falls back only the flagged capped bed package while keeping the unflagged Franka cylinder package in the Newton task smoke.
This is the behavior the acceptance layer is meant to expose: a selective fallback with a diagnostic reason, rather than a global ban on a primitive type.

\subsection{Collision-only throughput microbenchmark}
In one bed-aligned pressure scene with 32 accepted box primitives versus 32 convex64 mesh proxies, native primitives report median $2.21\times$ generated contacts per collide call and about $5.3\%$ lower collision-only wall time.
This result gives a reason to keep accepted native primitives in the pipeline: after a package passes the gate, primitive structure can matter for collision throughput.
The measurement remains a single collision-only pressure scene, so it motivates the acceptance-first design rather than a general speed claim.

\subsection{Phase 0 rigid-asset diagnostic table}
Table~\ref{tab:phase0-grscenes-rigid} moves from the mechanism case to a small multi-asset gate audit.
The recorded Phase~0 rigid run covers five materialized GRScenes assets, first-mesh bounding primitives, CPD-style packages, CoACD convex-mesh candidates, V-HACD candidates, and Newton contact, drop/settle, stack-or-slide, and sphere-rain probes.
The useful signal is not a win/loss ranking.
V-HACD generates packages for all five assets, yet the container, contact-affordance, and stackable rows still contain probe failures after Newton mapping.
This is exactly the distinction the method is designed to preserve: candidate availability is not task acceptance.
The same report family also records one Franka link-aware package generation record, one Franka USD articulation smoke, and one generated-package Franka task smoke; broader benchmark and robot-operation claims remain outside this small slice.

Figures~\ref{fig:phase0-overlays} and~\ref{fig:phase0-control-overlays} make the audit legible.
Each row starts from default/render mesh prims loaded through the repo-local USD mirror and then shows the candidate geometry consumed by the diagnostics.
The container, contact-affordance, and stackable rows matter because the failed V-HACD lanes are present, mapped packages rather than dependency gaps.
The keyboard row plays the opposite role: routine probes pass, but the precision policy still routes it to manual review, showing that the gate can preserve caution even without a red runtime cell.

\begin{figure}[tbp]
  \centering
  \includegraphics[width=\textwidth]{figures/generated/phase0_asset_package_overlays.pdf}
  \caption{Deterministic Phase~0 asset/package overlays for the representative rigid prop, container, and contact-affordance rows. Columns show the input mesh and four generated candidate lanes. High-count lanes draw a readable representative subset with a visible $+N$ marker; counts below each package remain primitive or hull counts in the recorded package, not claims of physical acceptance.}
  \label{fig:phase0-overlays}
\end{figure}

\begin{figure}[tbp]
  \centering
  \includegraphics[width=\textwidth]{figures/generated/phase0_asset_package_control_overlays.pdf}
  \caption{Additional Phase~0 asset/package overlay rows for the stackable tray and the precision negative-control keyboard. The keyboard input column shows aggregated USD default/render mesh prims for recognizability; high-count keyboard package cells draw a labeled representative local marker with a visible $+N$ overflow count. The split keeps the control rows readable while preserving the same candidate-lane ordering and recorded count semantics as Figure~\ref{fig:phase0-overlays}.}
  \label{fig:phase0-control-overlays}
\end{figure}

Figure~\ref{fig:phase0-outcome-matrix} expands the table into the full rigid diagnostic matrix.
The matrix is useful because it keeps three different events visually separate: a package can pass, fail after mapping, or be routed away as fallback.
Green cells show that simple packages can survive these smoke probes; red cells show that more detailed decompositions can still create unstable support, floor breach, or residual-motion labels.
The point is not to crown a baseline, but to make the acceptance decision auditable.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/generated/phase0_outcome_matrix.pdf}
  \caption{Outcome matrix for the scoped five-asset rigid run. Green cells are accepted probe outcomes, red cells are recorded failures, gray cells are explicit fallbacks, and light cells are not applicable. Fallback and failure are preserved as evidence rather than omitted from the table.}
  \label{fig:phase0-outcome-matrix}
\end{figure}

\begin{table}[tbp]
  \centering
  \footnotesize
  \begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.17\textwidth}>{\raggedright\arraybackslash}p{0.24\textwidth}>{\raggedright\arraybackslash}p{0.22\textwidth}>{\raggedright\arraybackslash}p{0.25\textwidth}@{}}
    \toprule
    Asset role & Candidate lanes & \simchecked{} outcome & Recorded fallback/failure \\
    \midrule
    rigid prop & bbox 1; CoACD 1; V-HACD 1; CPD 12 & generated lanes pass & single-convex fallback \\
    container & bbox 1; CoACD 9; V-HACD 16; CPD 16 & bbox pass; sphere pass & single-convex fallback; CoACD/CPD/V-HACD drop+stack failures \\
    contact affordance & bbox 1; CoACD 5; V-HACD 16; CPD 16 & bbox pass; sphere pass & single-convex fallback; CoACD/CPD/V-HACD drop; CPD stack failure \\
    stackable & bbox 1; CoACD 1; V-HACD 16; CPD 16 & bbox, CoACD, CPD pass & single-convex fallback; V-HACD drop failure \\
    precision control & bbox 1; CoACD 1; V-HACD 16; CPD 12 & tested lanes pass & single-convex fallback; manual review required \\
    \bottomrule
  \end{tabular}
  \caption{Scoped Phase~0 GRScenes diagnostic summary. Counts are primitive or hull counts in the recorded package lanes; fallback and failure entries are recorded outcomes, not hidden exclusions.}
  \label{tab:phase0-grscenes-rigid}
\end{table}

\FloatBarrier

\subsection{Collision-probe scene renderings}
\label{sec:exp-collision-scenes}

Figure~\ref{fig:phase0-collision-scenes} renders the diagnostic context for the three most
evidence-bearing rigid assets.
The left column shows the V-HACD package that was generated and mapped.
The middle column summarizes the drop/settle state transition using recorded initial and final
heights, and the right column summarizes the stack-or-slide probe.
For the bowl, both drop/settle and stack/slide reject the V-HACD package.
For the cylindrical contact prop and tray, the V-HACD stack/slide probe passes while drop/settle still rejects the
package.
That split is the central value of the checker: it localizes the physical question that failed while leaving passing probes visible.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.92\textwidth]{figures/generated/phase0_collision_probe_scenes.pdf}
  \caption{Collision-probe scene visualizations for the recorded V-HACD lanes on the container, cylindrical contact prop, and stackable tray rows. These panels are deterministic diagnostic renderings from the Phase~0 report, not photorealistic evidence. The key point is that a generated and mapped package can pass one task while failing another.}
  \label{fig:phase0-collision-scenes}
\end{figure}

The collision-scene panels also explain why the V-HACD failures are informative rather than merely negative.
If V-HACD had failed to run, the comparison would only expose a dependency gap.
Here the generated packages exist, map into Newton shapes, and then fail named probes.
The gate therefore turns a vague ``bad package'' into a task-level rejection.

\FloatBarrier

\subsection{Failure labels and measured symptoms}
\label{sec:exp-failure-labels}

Table~\ref{tab:phase0-failure-labels} lists the recorded failure rows behind the red cells in
Figure~\ref{fig:phase0-outcome-matrix}.
The labels are not post-hoc prose annotations; they are emitted by the probe code from measured
state.
For example, the bowl V-HACD drop/settle row records \texttt{no\_descent},
\texttt{not\_settled}, and \texttt{floor\_breach} together with a final speed of
$5.493$\,m/s.
The same bowl V-HACD package then fails stack/slide because the probe moves $1.219$\,m across the
support.
The cup V-HACD package shows a different failure: the final height is close to the plane, but the
final speed still exceeds the settle gate.
The tray V-HACD row is again different: the stack/slide probe accepts the package, while
drop/settle rejects it for residual motion.
The failure vocabulary is therefore more than bookkeeping: it tells a reviewer which physical assumption broke.

\begin{table}[!htbp]
  \centering
  \scriptsize
  \setlength{\tabcolsep}{2.2pt}
  \renewcommand{\arraystretch}{0.92}
  \begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.16\textwidth}>{\raggedright\arraybackslash}p{0.18\textwidth}>{\raggedright\arraybackslash}p{0.14\textwidth}>{\raggedright\arraybackslash}p{0.25\textwidth}>{\raggedright\arraybackslash}p{0.19\textwidth}@{}}
    \toprule
    Asset & Lane & Probe & Failure labels & Recorded symptom \\
    \midrule
    bowl & V-HACD & drop & no descent; not settled; floor breach & speed $5.493$\,m/s; final $z=0.517$ \\
    bowl & V-HACD & stack & excess slide; probe below support & slide $1.219$\,m; speed $0.018$\,m/s \\
    bowl & CoACD & drop & not settled; floor breach & speed $5.684$\,m/s; final $z=-0.138$ \\
    bowl & CoACD & stack & no final contact; excess slide; probe below support; not settled & slide $5.593$\,m; speed $9.373$\,m/s \\
    bowl & CPD-style & drop & not settled & speed $4.281$\,m/s; final $z=-0.851$ \\
    bowl & CPD-style & stack & excess slide; probe below support; not settled & slide $4.712$\,m; speed $9.703$\,m/s \\
    cup & V-HACD & drop & not settled & speed $0.086$\,m/s; final $z=-0.001$ \\
    cup & CoACD & drop & not settled; floor breach & speed $1.318$\,m/s; final $z=0.186$ \\
    cup & CPD-style & drop & not settled & speed $0.356$\,m/s; final $z=-0.016$ \\
    cup & CPD-style & stack & probe below support & slide $0.128$\,m; speed $0.002$\,m/s \\
    tray & V-HACD & drop & not settled & speed $0.204$\,m/s; final $z=0.007$ \\
    \bottomrule
  \end{tabular}
  \caption{Recorded failure labels and representative measured symptoms for the Phase~0 bowl, cup, and tray rows. These rows are generated-package diagnostic failures, not missing-baseline or dependency-gap cases.}
  \label{tab:phase0-failure-labels}
\end{table}

\FloatBarrier

\subsection{Franka articulation smoke}
The Franka smoke run adds a different kind of acceptance condition: for articulated assets, the package must preserve ownership before contact behavior is even meaningful.
The USD smoke asset provides the articulated test case.
The link-aware package generator detects 12 rigid-body links and 12 joint edges, emits 12 link-framed box primitives, and records zero cross-link merges.
The \texttt{panda\_link8} primitive is an explicit meshless sentinel because that link has no mesh in the source USD mirror.
The articulation smoke records joint-tree import, a short gravity hold, and a simple kinematic trajectory as passed.
The generated-package task smoke separately suppresses source USD shapes, attaches all 12 generated package primitives to Newton bodies, records zero missing body links, and passes the same short hold/trajectory probe.
This shows package consumption for one Franka smoke asset; contact-operation and manipulation behavior require broader robot probes.

\begin{figure}[tbp]
  \centering
  \includegraphics[width=0.98\textwidth]{figures/generated/franka_link_aware_rtx_task_scene.pdf}
  \caption{RTX view of the single Franka link-aware generated-package smoke. The visual plate shows the recorded link-framed package in the Newton task context with source USD collision shapes suppressed, 12 generated box primitives attached to 12 detected links, zero missing body links, and 66 generated self-collision filter pairs for the smoke configuration. This supports bounded package-consumption exposition only; it is not whole-robot collision-quality or manipulation evidence.}
  \label{fig:franka-task-scene}
\end{figure}

Primitive packages for articulated assets must preserve link identity, because a visually plausible
robot proxy can be unusable if it merges geometry across joints.
The link-boundary check therefore runs before task execution: every primitive must
have one source link, use that link as its frame, and avoid cross-link merges.
The subsequent generated-package task smoke then records package consumption by Newton bodies with
source USD collision shapes suppressed.
The recorded task is intentionally short, but it completes the paper's accounting chain: source link
identity, generated primitive ownership, Newton body attachment, and task consumption are all visible
as separate checks.
