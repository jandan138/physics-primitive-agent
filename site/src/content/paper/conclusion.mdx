---
title: "Discussion, Limitations, And Conclusion"
slug: "conclusion"
---
import PaperBlock from "../../components/PaperBlock.astro";
import FigurePanel from "../../components/FigurePanel.astro";
import LatexBlock from "../../components/LatexBlock.astro";
import EquationBlock from "../../components/EquationBlock.astro";

# Discussion, Limitations, And Conclusion

## Discussion

<PaperBlock
  id="conclusion-p001"
  section="Discussion, Limitations, And Conclusion"
  original="Our approach decomposes an input mesh into a number of convex primitives, suitable for rigid-body collision, drastically and robustly simplifying complex real-world meshes. Convex primitive decomposition scales with mesh size and can handles millions of triangles."
  translation="我们的方法将输入网格分解为若干适用于刚体碰撞的凸基元，能够大幅且鲁棒地简化复杂的真实世界网格。凸基元分解随网格规模扩展，并且能够处理数百万个三角形。"
  translationStatus="draft_ai_assisted"
  explanationStatus="paragraph"
  reproductionStatus="not_started"
  sourceNamespace="cpd_paper_source_text"
  translationProvenance="codex_draft_2026-05-15"
/>

<PaperBlock
  id="conclusion-p002"
  section="Discussion, Limitations, And Conclusion"
  original="While previous approaches focus on tight decompositions using convex hulls, they are impractical due to slower simulation and imprecision due to voxelization and planar cuts. We flip the top-down approach with bottom-up merging, removing the complexity of cutting while producing efficient decompositions."
  translation="以往方法关注使用凸包进行紧密分解，但由于仿真较慢，以及体素化和平面切割带来的不精确，这些方法并不实用。我们将自顶向下的方法反转为自底向上合并，在生成高效分解的同时移除了切割的复杂性。"
  translationStatus="draft_ai_assisted"
  explanationStatus="paragraph"
  reproductionStatus="not_started"
  sourceNamespace="cpd_paper_source_text"
  translationProvenance="codex_draft_2026-05-15"
/>

<PaperBlock
  id="conclusion-p003"
  section="Discussion, Limitations, And Conclusion"
  original="Unlike prior approaches in primitive abstraction, we do not use deep learning, RANSAC, or differentiability, making our approach robust and fast for quick artistic iteration. Our approach can be built into existing tooling for artists to quickly generate collision objects which can then be directly manipulated."
  translation="不同于既有的基元抽象方法，我们不使用深度学习、RANSAC 或可微性，因此我们的方法对于快速美术迭代而言鲁棒且快速。我们的方法可以集成到现有美术工具中，使美术人员能够快速生成碰撞对象，并随后直接操作这些对象。"
  translationStatus="draft_ai_assisted"
  explanationStatus="paragraph"
  reproductionStatus="not_started"
  sourceNamespace="cpd_paper_source_text"
  translationProvenance="codex_draft_2026-05-15"
/>

## Limitations

<PaperBlock
  id="conclusion-p004"
  section="Discussion, Limitations, And Conclusion"
  original="One limitation of our approach is meshes with internal components will have coarser bounding primitives, because normals of enclosed faces affect the eigendecomposition of each primitive. We show an example of this failure case in Fig. cube-failure on a cube with twisted cubes inside of it. It is clear that the tightest bounding box only needs to fit the largest cube, but our approach does not recover that due to the influence of the other cubes."
  translation="我们方法的一个局限是，对于包含内部组件的网格，包围基元会更粗糙，因为被包围面的法线会影响每个基元的特征分解。我们在 Fig. cube_failure 中展示了这种失败案例：一个内部包含扭转立方体的立方体。显然，最紧的包围盒只需要拟合最大的立方体，但由于其他立方体的影响，我们的方法无法恢复这一结果。"
  translationStatus="draft_ai_assisted"
  explanationStatus="paragraph"
  reproductionStatus="not_started"
  sourceNamespace="cpd_paper_source_text"
  translationProvenance="codex_draft_2026-05-15"
/>

<FigurePanel id="conclusion-l001-figure"
  title="Discussion, Limitations, And Conclusion / figure / conclusion-l001"
  caption="While our approach can decompose each component of the input well separately (middle), when combining them it does not recover a tight bounding box (right). This is because internal components unnecessarily affect the orientation of the output primitive. License: deslancer."
  images={["paper-assets/ablations/cube2_input_ablation.webp", "paper-assets/ablations/cube2_ablation.webp", "paper-assets/ablations/cube2_1_prim_ablation.webp"]}
/>

<PaperBlock
  id="conclusion-p005"
  section="Discussion, Limitations, And Conclusion"
  original="While our approach can decompose each component of the input well separately (middle), when combining them it does not recover a tight bounding box (right). This is because internal components unnecessarily affect the orientation of the output primitive. License: deslancer."
  translation="虽然我们的方法能够分别较好地分解输入的每个组件（中），但在将它们合并时无法恢复紧密的包围盒（右）。这是因为内部组件会不必要地影响输出基元的朝向。许可证：deslancer。"
  translationStatus="draft_ai_assisted"
  explanationStatus="caption"
  reproductionStatus="not_started"
  sourceNamespace="cpd_paper_source_text"
  translationProvenance="codex_draft_2026-05-15"
/>

<PaperBlock
  id="conclusion-p006"
  section="Discussion, Limitations, And Conclusion"
  original="Another limitation is that primitives that subsume coplanar faces may be degenerate and depend on the input mesh's orientation. This can be mitigated by adding the tangent direction (Sec. ablate-tangent-quadric), but does not always remove the influence of the input orientation. Furthermore, perfectly coplanar faces may be overeagerly merged together. This is because these faces have no width along their shared plane, introducing near-zero volume when they are merged."
  translation="另一个局限是，吸收共面面的基元可能退化，并依赖于输入网格的朝向。可以通过加入切向方向（Sec. ablate-tangent-quadric）来缓解这一问题，但并不总能消除输入朝向的影响。此外，完全共面的面可能会被过度积极地合并。这是因为这些面沿其共享平面没有宽度，在合并时会引入接近零的体积。"
  translationStatus="draft_ai_assisted"
  explanationStatus="paragraph"
  reproductionStatus="not_started"
  sourceNamespace="cpd_paper_source_text"
  translationProvenance="codex_draft_2026-05-15"
/>

<PaperBlock
  id="conclusion-p007"
  section="Discussion, Limitations, And Conclusion"
  original="One limitation due to the coarseness of primitives is that they may be less suitable for high frequency details. For organic objects which often have more detail and curvature, primitive decomposition must output more primitives to maintain geometric similarity. As one future direction, this approach could be extended with curved primitives, or for those portions of the mesh convex hulls can be used to preserve detail."
  translation="由于基元较为粗略，一个局限是它们可能不太适合高频细节。对于通常具有更多细节和曲率的有机物体，基元分解必须输出更多基元才能保持几何相似性。作为未来方向之一，该方法可以扩展为使用曲面基元，或者在网格的这些部分使用凸包来保留细节。"
  translationStatus="draft_ai_assisted"
  explanationStatus="paragraph"
  reproductionStatus="not_started"
  sourceNamespace="cpd_paper_source_text"
  translationProvenance="codex_draft_2026-05-15"
/>

<PaperBlock
  id="conclusion-p008"
  section="Discussion, Limitations, And Conclusion"
  original="Finally, it may be considered a limitation that convex primitive decomposition relies on the topology of the input. Small modifications in the topology of the input mesh can change the output mesh as can be seen by vertex deduplication in Fig. ablate-vertex-merging. We do not find this to be a problem, and perform vertex deduplication and pairwise collapse to mitigate the effects of topology. Often, topology also serves as a good indicator as to where to split the mesh into different components, so it can be considered a way for artists to indicate where to separate colliders."
  translation="最后，凸基元分解依赖输入拓扑这一点也可能被视为局限。输入网格拓扑上的小修改会改变输出网格，这一点可以从 Fig. ablate-vertex-merging 中的顶点去重看出。我们并不认为这是问题，并且会执行顶点去重和成对折叠来缓解拓扑影响。通常，拓扑也能很好地指示应在何处将网格拆分为不同组件，因此也可以被视为美术人员指示碰撞体分离位置的一种方式。"
  translationStatus="draft_ai_assisted"
  explanationStatus="paragraph"
  reproductionStatus="not_started"
  sourceNamespace="cpd_paper_source_text"
  translationProvenance="codex_draft_2026-05-15"
/>

<PaperBlock
  id="conclusion-p009"
  section="Discussion, Limitations, And Conclusion"
  original="Future Work: We are able to handle cases where disconnected components of the mesh are fused together, using something similar to non-edge contraction from Garland and Heckbert [qem]. In collision generation though, disconnected components may be more amenable to merging as compared to mesh simplification. We leave it to future work to explore edge collapses not defined by the input topology, using techniques such as the one from Liu et al. [simplifying_wild_tris]."
  translation="未来工作：我们能够处理网格中断开组件被融合在一起的情况，所用方法类似于 Garland 和 Heckbert [qem] 的非边收缩。不过在碰撞生成中，与网格简化相比，断开组件可能更适合被合并。我们将探索不由输入拓扑定义的边折叠留作未来工作，可使用 Liu 等人 [simplifying_wild_tris] 的技术。"
  translationStatus="draft_ai_assisted"
  explanationStatus="paragraph"
  reproductionStatus="not_started"
  sourceNamespace="cpd_paper_source_text"
  translationProvenance="codex_draft_2026-05-15"
/>

## Conclusion

<PaperBlock
  id="conclusion-p010"
  section="Discussion, Limitations, And Conclusion"
  original="In summary, we develop a concise and robust algorithm for decomposing an arbitrary mesh into a number of convex primitives usable in downstream applications such as collision detection. Our approach produces a small number of tight-fitting primitives designed for efficiency at runtime. We hope that our approach motivates further work into simple algorithms that closely align with the processes artists take, and that this work is adapted into game engines and physics/collider generation tooling."
  translation="总之，我们开发了一种简洁且鲁棒的算法，可将任意网格分解为若干可用于碰撞检测等下游应用的凸基元。我们的方法生成少量紧密拟合的基元，并面向运行时效率而设计。我们希望该方法能够推动更多与美术人员实际流程紧密一致的简单算法研究，并被集成到游戏引擎以及物理/碰撞体生成工具中。"
  translationStatus="draft_ai_assisted"
  explanationStatus="paragraph"
  reproductionStatus="not_started"
  sourceNamespace="cpd_paper_source_text"
  translationProvenance="codex_draft_2026-05-15"
/>
