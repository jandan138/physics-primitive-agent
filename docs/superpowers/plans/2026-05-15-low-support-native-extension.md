# Low-Support Native Extension Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a controlled support-aware primitive-selection slice for low-support Newton-native extension candidates.

**Architecture:** Keep candidate fitting unchanged, add admissibility-aware ranking at the primitive selection boundary, then surface that ranking in synthetic and real-USD candidate-loss reports. Documentation records the slice as diagnostic accounting only.

**Tech Stack:** Python package under `src/primitive_collision_compiler`, pytest, Markdown records/docs, existing CLI report commands.

---

### Task 1: Failing Selection Tests

**Files:**
- Modify: `tests/test_cpd_like_synthetic.py`
- Modify: `tests/test_real_usd_native_comparison.py`

- [ ] Add a low-support patch test where a cheaper `cylinder` candidate loses to `box` because it has too few faces/points.
- [ ] Add a candidate-loss diagnosis test where a cheaper extension is labeled support-blocked rather than selection-inconsistent.
- [ ] Run targeted tests and verify they fail for the expected missing support-aware fields/rules.

### Task 2: Support-Aware Selection

**Files:**
- Modify: `src/primitive_collision_compiler/baselines/cpd_like/primitives.py`
- Modify: `src/primitive_collision_compiler/baselines/cpd_like/synthetic.py`
- Modify: `src/primitive_collision_compiler/baselines/cpd_like/real_usd_comparison.py`

- [ ] Add primitive-selection admissibility helpers for `cylinder`, `cone`, and `ellipsoid`.
- [ ] Update `fit_best_primitive` to rank admissible candidates before lower-cost inadmissible extension candidates when a fallback exists.
- [ ] Add candidate audit fields: `selection_admissible`, `selection_admissibility_reason`, and support thresholds.
- [ ] Update candidate-loss labels for cheaper but support-blocked extension candidates.
- [ ] Run targeted tests and verify the new tests pass.

### Task 3: Docs And Records

**Files:**
- Modify: `docs/reference/cpd-latest-diagnostic-loop-explainer.md`
- Modify: `docs/reference/cpd-paper-story-status.md`
- Modify: `docs/reference/claim-boundaries.md`
- Modify: `experiments/registry.yaml`
- Modify: `docs/records/README.md`
- Create: `docs/records/2026-05-15-low-support-native-extension.md`

- [ ] Explain the low-support native-extension patch in the CPD paper story.
- [ ] Record that the slice is support-aware diagnostic selection, not paper-faithful optimization.
- [ ] Add or update claim-boundary bullets for the new report fields and forbidden claims.
- [ ] Link the dated record from the records index and experiment registry.

### Task 4: Regenerate Reports And Verify

**Files:**
- Existing report outputs generated by CLI commands, if the current scripts write committed summaries.

- [ ] Run synthetic native fitting comparison.
- [ ] Run real-USD candidate-loss diagnosis for bed and Franka.
- [ ] Run real-USD native fitting/contact/task comparisons if dependencies are available.
- [ ] Run focused pytest, full pytest, docs validation, site-claims validation if present, and `git diff --check`.
- [ ] Review diffs for accidental `site/` changes or raw asset/log additions.

### Task 5: Review

**Files:**
- All files changed by this slice.

- [ ] Incorporate read-only agent findings.
- [ ] Request implementation review after tests are green.
- [ ] Fix Critical/Important feedback before final response.
